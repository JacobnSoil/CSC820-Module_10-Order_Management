apiVersion: v1
kind: Service
metadata:
  name: order-management-service
spec:
  selector:
    app: order-management
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 3000